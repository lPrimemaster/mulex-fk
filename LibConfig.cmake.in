@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
# Specify dependencies that must exist
find_dependency(ZLIB REQUIRED)
find_dependency(OpenSSL REQUIRED)
find_dependency(Tracy REQUIRED)
find_dependency(base64 REQUIRED)
if(WIN32)
	find_dependency(libuv REQUIRED CONFIG)
# else()
# 	find_dependency(libuv REQUIRED)
endif()


include(${CMAKE_CURRENT_LIST_DIR}/@LIB_TARGET@Targets.cmake)
